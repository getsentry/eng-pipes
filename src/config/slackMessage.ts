export enum SlackMessage {
  REQUIRED_CHECK = 'required-check',
  PLEASE_DEPLOY = 'please-deploy',
  FEED_ENG_DEPLOY = 'feed-eng-deploy',
  FEED_DEV_PROD_GOCD_DEPLOY = 'feed-dev-productivity-gocd-deploy',
}
